import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const ThemeAnimation = (props: Props) => {
  return (
    <View>
      <Text>ThemeAnimation</Text>
    </View>
  );
};

export default ThemeAnimation;

const styles = StyleSheet.create({});
